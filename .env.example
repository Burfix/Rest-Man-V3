# ------------------------------------------------------------
# Compliance Engine — https://vna-compliance.vercel.app
# Separate system. Override if using a custom subdomain.
# ------------------------------------------------------------

# Public URL for the Compliance Engine portal (safe to expose to browser).
# Used in /dashboard/compliance-engine to build external links.
NEXT_PUBLIC_COMPLIANCE_URL=https://vna-compliance.vercel.app

# ============================================================
# Si Cantina Sociale — Concierge Environment Variables
# ============================================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.

# ------------------------------------------------------------
# Supabase — https://supabase.com
# ------------------------------------------------------------

# Public URL (safe to expose to browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Anon key (safe to expose to browser — restricted by RLS)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Service Role key — NEVER expose to browser
# Used in server-side code only (API routes, server components)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ------------------------------------------------------------
# Google Places API — live Google reviews on /dashboard/reviews
# https://console.cloud.google.com → APIs & Services → Places API
# ------------------------------------------------------------

# Server-side only key — NEVER expose to browser
GOOGLE_PLACES_API_KEY=your_google_places_api_key

# Optional: hard-code after first deploy (check Vercel function logs for
# the line: [GoogleReviews] Discovered Place ID: ChIJ...)
# Avoids a findplacefromtext call on every cold start.
GOOGLE_PLACE_ID=

# ------------------------------------------------------------
# OpenAI — https://platform.openai.com
# ------------------------------------------------------------

OPENAI_API_KEY=sk-your_openai_api_key

# ------------------------------------------------------------
# Oracle MICROS BI — Live POS Integration
# Auth flow: PKCE (Proof Key for Code Exchange) — no client secret required.
# ------------------------------------------------------------

# Feature flag — set to "true" to activate live MICROS data.
# When false (default), the system uses cached/existing data.
# NEVER set this to true until all required vars below are configured.
MICROS_ENABLED=false

# Oracle OIDC provider (IDM) base URL — no trailing slash.
# For Oracle MSAF: https://ors-idm.msaf.oraclerestaurants.com
MICROS_AUTH_SERVER=

# MICROS BI Application Server base URL — no trailing slash.
# For Oracle MSAF: https://simphony-home.msaf.oraclerestaurants.com
MICROS_BI_SERVER=

# Registered OIDC public client ID (from Oracle IDM app registration).
# No client secret is required — the integration uses PKCE flow.
MICROS_CLIENT_ID=

# Oracle org / tenant short name (e.g. "SCS").
# Used as x-app-key header and in PKCE scope.
MICROS_ORG_SHORT_NAME=

# BI API account username — Oracle IDM account for API access.
# SERVER-SIDE ONLY — store in Vercel environment variables.
MICROS_USERNAME=

# BI API account password — SERVER-SIDE ONLY, NEVER expose to browser.
# Quote the value if it contains special characters like # or !
# Store this in Vercel environment variables (not in git).
MICROS_PASSWORD=

# Location reference for the pilot store (passed as locRef in every API call).
# Typically a short integer or store code string from your Simphony config.
MICROS_LOCATION_REF=

# Optional: PKCE redirect URI (default: "apiaccount://callback").
# MICROS_REDIRECT_URI=apiaccount://callback

# Optional: override default Oracle BI API endpoint paths.
# Only set these if your Oracle Simphony deployment uses non-standard paths.
# MICROS_PATH_DAILY_TOTALS=/rms/v1/reports/dailyBusinessSummary
# MICROS_PATH_INTERVALS=/rms/v1/reports/salesByInterval
# MICROS_PATH_GUEST_CHECKS=/rms/v1/guestChecks

# Deprecated aliases — still accepted for backward compatibility:
# MICROS_APP_SERVER     → MICROS_BI_SERVER
# MICROS_ORG_IDENTIFIER → MICROS_ORG_SHORT_NAME
# MICROS_API_ACCOUNT_NAME → MICROS_USERNAME
# MICROS_LOC_REF        → MICROS_LOCATION_REF

# ------------------------------------------------------------
# Primi Camps Bay MICROS BI — OAuth2 Client Credentials Grant
# Auth flow: client_credentials (no username/password required).
# Uses HTTP Basic auth: base64(clientId:clientSecret).
# ALL vars below are SERVER-SIDE ONLY — never prefix with NEXT_PUBLIC_.
# ------------------------------------------------------------

# Feature flag — set to "true" once all vars below are configured.
MICROS_PRIMI_CAMPS_BAY_ENABLED=false

# Oracle enterprise short name (e.g. "PRI").
# Used in the BIAPI base path: {BASE_URL}/bi/v1/{ENTERPRISE}/...
MICROS_PRIMI_CAMPS_BAY_ENTERPRISE=PRI

# Oracle OIDC provider (IDM) base URL — no trailing slash.
MICROS_PRIMI_CAMPS_BAY_AUTH_URL=https://ors-idm.msaf.oraclerestaurants.com

# MICROS BI Application Server base URL — no trailing slash.
MICROS_PRIMI_CAMPS_BAY_BASE_URL=https://simphony-home.msaf.oraclerestaurants.com

# Registered OAuth2 client ID from Oracle IDM.
MICROS_PRIMI_CAMPS_BAY_CLIENT_ID=UFJJLmQ1Zjg3YjNlLWM4MmItNDM0ZC05OTZlLWM5NzVlZjVjN2VhYQ

# API account username (e.g. PRI_THAMSANQA_BIAPI) — same PKCE flow as Si Cantina.
MICROS_PRIMI_CAMPS_BAY_USERNAME=PRI_THAMSANQA_BIAPI

# API account password — SERVER-SIDE ONLY, NEVER expose to browser.
# Stored as CLIENT_SECRET for backward compatibility with env naming.
# Quote the value if it contains special characters like # or !
MICROS_PRIMI_CAMPS_BAY_CLIENT_SECRET=   # never commit the real value

# Location reference (locRef) for the Camps Bay store.
# Obtain from your Simphony configuration or Oracle support.
MICROS_PRIMI_CAMPS_BAY_LOCATION_REF=    # e.g. 100123

# ------------------------------------------------------------
# Oracle MICROS Inventory Management — SOAP / RNA Credentials
# Separate from the BI API OAuth flow above. Uses username + password
# in a SOAP AuthenticationHeader for the POS Web Services API.
# ------------------------------------------------------------

# Inventory API base URL (may be the same as MICROS_BI_SERVER)
MICROS_INV_APP_SERVER_URL=

# RNA username for the SOAP AuthenticationHeader
MICROS_INV_USERNAME=

# RNA password — SERVER-SIDE ONLY, NEVER expose to browser.
# Quote the value if it contains special characters like @ # ! *
MICROS_INV_PASSWORD=

# ------------------------------------------------------------
# WhatsApp Cloud API — https://developers.facebook.com/docs/whatsapp
# ------------------------------------------------------------

# Phone Number ID from Meta Business Manager
WHATSAPP_PHONE_NUMBER_ID=your_whatsapp_phone_number_id

# Permanent access token (or system user token for production)
WHATSAPP_ACCESS_TOKEN=your_whatsapp_access_token

# Webhook verify token — choose any random string; set the same
# value in Meta Developer Console > WhatsApp > Webhooks
WHATSAPP_WEBHOOK_VERIFY_TOKEN=your_random_verify_token

# ------------------------------------------------------------
# Sentry — Error Tracking & Performance Monitoring
# https://sentry.io  →  Settings → Projects → [project] → Client Keys
#
# Vercel dashboard: set SENTRY_AUTH_TOKEN for source-map uploads.
# All other vars must be set in both Production and Preview environments.
# ------------------------------------------------------------

# Public DSN — used in the browser bundle (safe to expose)
NEXT_PUBLIC_SENTRY_DSN=https://YOUR_KEY@oXXXXXX.ingest.sentry.io/XXXXXXX

# Server / Edge DSN — same value as above, server-side only env var
SENTRY_DSN=https://YOUR_KEY@oXXXXXX.ingest.sentry.io/XXXXXXX

# Auth token for uploading source maps during `npm run build`
# Generate at: https://sentry.io/settings/account/api/auth-tokens/
# Required permission: project:releases
SENTRY_AUTH_TOKEN=sntrys_your_auth_token_here

# Sentry organisation slug (from your Sentry org URL)
SENTRY_ORG=your-org-slug

# Sentry project slug
SENTRY_PROJECT=your-project-slug

# Optional: transaction sample rate (default 0.1 = 10%)
# Increase to 1.0 for debugging, lower for high-traffic production
NEXT_PUBLIC_SENTRY_SAMPLE_RATE=0.1

# ------------------------------------------------------------
# Upstash Redis — Caching Layer
# https://console.upstash.com  →  Create Database → REST API
#
# Used for: brain output cache, context cache, site config cache.
# Free tier: 10,000 commands/day — upgrade before reaching 10 pilot sites.
# Set in Vercel: Production + Preview environments.
# ------------------------------------------------------------

# REST URL for the Upstash Redis database
UPSTASH_REDIS_REST_URL=https://your-db-name.upstash.io

# REST token (read-write) — SERVER-SIDE ONLY, NEVER expose to browser
UPSTASH_REDIS_REST_TOKEN=your_upstash_rest_token
